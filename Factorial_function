#include <iostream>
using namespace std;

int factorial(int n)
{
    int fact=n;
    if(n>1)
    {
    n--;
    fact=fact*factorial(n);
    }
    return fact;
}

int main()
{
    int n; cin>>n;
    cout<<factorial(n);
}
